# Public site origin (https only, no trailing slash). Required for link previews
# (iMessage, Messages, social) to use an absolute image URL. Set the same in
# Cloudflare Pages / Workers: Build > Environment variables > VITE_PUBLIC_SITE_URL
# VITE_PUBLIC_SITE_URL=https://www.your-domain.com
